import React from 'react';
import { connect } from 'react-redux';

const ConversationCount = props => {
  return <div>Conversation Count</div>;
};

export default connect(mapStateToProps)(ConversationCount);
